services:
  - type: web
    name: tracker-gastos-tarjeta
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production 